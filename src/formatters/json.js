/**
 * Converts the provided data object to a JSON-formatted string.
 *
 * @param {any} data - The data object to be converted to JSON.
 * @returns {string} A JSON-formatted string representing the provided data.
 */

const jsonFormat = data => JSON.stringify(data)

export default jsonFormat
